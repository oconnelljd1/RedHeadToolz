using UnityEngine.SceneManagement;

namespace RedHeadToolz.Utils
{
    public static class SceneDataFactory
    {
        public static void CreateData()
        {
            // collect scenes
            // for each scenes


            Scene scene;
            // string GUID = scene.GUID;

            // create new SceneData SO
            // Name it = scene.name
            // pass these two into it's constructor / init function

            // Scene's won't be remembered once i make a build
            // So i need to only remember the scenes guid, name and build order?
            // Sounds easy enough
        }
    }
}